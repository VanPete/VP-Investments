# Deprecated: Use vp-pipeline.yml instead. This file intentionally left blank.
name: Deprecated (no-op)

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use vp-pipeline.yml."
