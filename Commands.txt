# Default cleanup (see note about DB wipe below)
python tools\cleanup.py

#Fresh start (clears previous output data and backtest.db; keeps weights)
python tools\cleanup.py --fresh-start

# Hard reset
python tools\cleanup.py --hard-reset

# Hard reset but keep weights
python tools\cleanup.py --hard-reset --keep-weights

#timestamp